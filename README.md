# talkincode/homebrew-tap

Homebrew tap for talkincode projects.

## Usage

```bash
brew install talkincode/tap/sshx
```

Formulae in this tap are rendered and pushed automatically by each
project's release workflow. See `talkincode/sshx`'s `RELEASE.md` for the
`sshx` formula's publishing process.
